# Builds the Quarto site on every push to `source` and publishes _site/ to gh-pages.

name: GitHub Pages

on:
  push:
    branches:
      - source
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install Python dependencies with uv
        run: uv sync

      - name: Install Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: 1.10.18
          tinytex: true

      - name: Set site URL from the SITE_DOMAIN repository variable
        if: ${{ vars.SITE_DOMAIN != '' }}
        shell: bash
        run: |
          sed -i "s|^  site-url: .*|  site-url: \"https://${{ vars.SITE_DOMAIN }}\"|" _quarto.yml
          grep -n "site-url" _quarto.yml

      - name: Render Quarto project
        shell: bash
        run: |
          source .venv/bin/activate
          export QUARTO_PYTHON=$VIRTUAL_ENV/bin/python
          quarto render

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        if: ${{ github.ref == 'refs/heads/source' }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./_site
          # Custom domain comes from the SITE_DOMAIN repository variable (Settings > Secrets and variables > Actions)
          cname: ${{ vars.SITE_DOMAIN }}
          # Keep gh-pages as a single commit; build output needs no history
          force_orphan: true
